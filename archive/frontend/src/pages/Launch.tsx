/**
 * ARCHIVED (legacy):
 * This page implemented a legacy "Launch" flow that depended on `VaultActivator`.
 *
 * The app has cut over to the phased `/deploy` flow (`DeployVault.tsx`) and the AA activation flow
 * (`LaunchVaultAA.tsx`).
 */

export function Launch() {
  throw new Error('Archived page: use /deploy')
}

