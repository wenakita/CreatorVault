# Archived: `docs/deployment/launch/LAUNCH_CHECKLIST.md`

This checklist described the legacy `VaultActivator` launch path.

- **Status**: archived (legacy)
- **Replacement**: activation should go through `VaultActivationBatcher` / `LaunchVaultAA`, and new deployments through `/deploy`.
- **Full content**: available in git history prior to archival (2026-01-19).

