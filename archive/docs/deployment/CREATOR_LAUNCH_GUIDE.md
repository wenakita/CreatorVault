# Archived: `docs/deployment/CREATOR_LAUNCH_GUIDE.md`

This guide referenced the legacy `CreatorVaultBatcher` deploy+launch entrypoints and pre-cutover infra addresses.

- **Status**: archived (legacy)
- **Replacement**: phased deploy flow (`CreatorVaultDeployer`) + current frontend deploy UX.
- **Full content**: available in git history prior to archival (2026-01-19).

