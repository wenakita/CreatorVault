# Archived: `docs/deployment/PRODUCTION_DEPLOYMENT.md`

This doc referenced legacy infra scripts and the legacy `CreatorVaultBatcher` address/config prior to the phased deploy cutover.

- **Status**: archived (legacy)
- **Replacement**: current deployment runbooks should reference `CreatorVaultDeployer` and the current Base defaults in `frontend/src/config/contracts.defaults.ts`.
- **Full content**: available in git history prior to archival (2026-01-19).

