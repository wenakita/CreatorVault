import type { ActionKey } from './waitlistTypes'

export const ACTION_POINTS: Record<ActionKey, number> = {
  shareX: 10,
  copyLink: 5,
  share: 7,
  follow: 4,
  saveApp: 6,
}

export const SIGNUP_POINTS = 1
